# Empty Capacitor webDir
This folder exists so `cap sync` has somewhere to copy fallback assets
when running in wrapper mode (the native shell loads the production
web URL from `capacitor.config.ts`).
