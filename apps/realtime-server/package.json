{
    "name": "@notai/realtime-server",
    "version": "0.1.3",
    "private": true,
    "type": "module",
    "main": "src/index.ts",
    "scripts": {
        "dev": "tsx watch --env-file=../web/.env.local src/index.ts",
        "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=esm --packages=external --external:@notai/* --outfile=dist/index.js && esbuild src/index.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/bundle.mjs",
        "start": "node dist/bundle.mjs",
        "typecheck": "tsc --noEmit",
        "clean": "rimraf dist .turbo"
    },
    "dependencies": {
        "@hocuspocus/extension-database": "catalog:",
        "@hocuspocus/extension-logger": "catalog:",
        "@hocuspocus/server": "catalog:",
        "@notai/db": "workspace:*",
        "@notai/lib": "workspace:*",
        "yjs": "catalog:"
    },
    "devDependencies": {
        "@types/node": "catalog:",
        "esbuild": "^0.25.0",
        "rimraf": "^6.0.1",
        "tsx": "catalog:",
        "typescript": "catalog:"
    }
}